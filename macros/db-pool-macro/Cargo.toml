[package]
name = "saps-db-pool-macro"
version = "0.1.0"
edition = "2024"
description = "Procedural macro for defining PostgreSQL connection pools"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
quote = { workspace = true }
syn = { workspace = true }
proc-macro2 = { workspace = true }
